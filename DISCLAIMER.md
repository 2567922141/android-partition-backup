# 免责声明（DISCLAIMER）

> 本文档是 [LICENSE](LICENSE)（GNU General Public License v3.0）的**补充说明**，
> 针对本工具的特殊风险作出提示。
>
> 它**不修改、不替代、不豁免** GPL-3.0 的任何条款 —— 法律效力以 `LICENSE` 为准。
> 本文档的作用是把「用这个工具可能出什么事」讲清楚。

---

## 一、本工具做什么

本工具用于**读取**安卓设备的分区数据，把「丢了就再也找不回来」的分区备份到电脑。

**所有备份操作都是只读的** —— 工具本身不会向设备分区写入任何数据。

## 二、但恢复操作是危险的

**【恢复】功能本工具不提供。** 工具只在生成的备份文档里给出**参考命令**，
需要使用者自己手工执行。

写分区是**变砖的唯一途径**。手工恢复可能导致：

| 后果 | 说明 |
|---|---|
| 🧱 **设备无法启动（变砖）** | 写错分区、算错偏移、写坏 boot/init_boot 等 |
| 📵 **IMEI / 基带丢失** | 写坏 `modemst1` / `modemst2` / `fsg`，失去移动网络 |
| 👆 **硬件功能异常** | 指纹、屏幕校准、摄像头、传感器失效 |
| 🧾 **保修失效** | 解锁 BL 与 root 本身即会导致保修失效 |

## 三、三条铁律（务必遵守）

### 1️⃣ 严禁跨设备恢复

`persist` / `modemst*` / `efs` / `nvdata` 等分区含**【逐台唯一】**的产线校准数据与身份标识。

恢复他人分享的备份会导致：
- 指纹失效、信号异常
- **双机 IMEI 冲突**（违法）
- 甚至**再也无法解锁 BL**

> 每一台设备都必须使用**自己**导出的备份。

### 2️⃣ `secdata` 是单向防回滚计数器

它是故意设计成**不可逆**的安全机制。恢复旧值可能触发**熔断**，
导致设备永久无法回滚到旧版本固件。

### 3️⃣ 手工 `dd` 前必须确认逻辑扇区大小

不同设备可能是 **512** 或 **4096** 字节。算错偏移会**彻底破坏分区表**。

> 本项目实测的 Redmi K70（高通 UFS）就是 **4096 字节**逻辑扇区 ——
> GPT 头在偏移 **4096**，保护性 MBR 签名 `55AA` 在偏移 **510**，两者不同。

## 四、软件"按原样"提供

本软件按 **"原样"（AS IS）** 提供，不提供任何形式的担保。

**作者不对任何设备损坏、数据丢失或其他损失承担责任。**

使用本工具即表示你已阅读、理解并接受上述全部风险，**后果自负**。

---

## 五、关于 AI 生成

本项目是 **100% Vibe Coding 项目** —— 由「人类提需求 + AI 实现」的结对方式完成。

> ⚠️ **AI 有可能犯错。** 仅供 vibe coding 学习用途，
> 请勿用于工业生产以及其他重要行业中。若出现任何损失，后果自负！！！

所有代码都经过真机（Redmi K70）验证，**其他手机请自行评估风险**后再用于备份重要数据。
