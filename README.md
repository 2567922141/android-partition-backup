# 安卓分区备份工具 (Android Partition Backup)

[![中文](https://img.shields.io/badge/README-%E4%B8%AD%E6%96%87-d73a49?style=flat-square)](README.md) [![English](https://img.shields.io/badge/README-English-0969da?style=flat-square&logo=readthedocs&logoColor=white)](README.en.md)

> 🌐 **本页是中文版。** 看不惯中文？ → **[Read the English documentation (README.en.md)](README.en.md)**

[![Vibe Coding](https://img.shields.io/badge/Vibe%20Coding-100%25-ff69b4?style=flat-square)](#-关于本项目)
[![Made with AI](https://img.shields.io/badge/Made%20with-AI%20pair%20programming-8a2be2?style=flat-square)](#-关于本项目)
[![Python](https://img.shields.io/badge/Python-3.8%2B%20%7C%20%E9%9B%B6%E4%BE%9D%E8%B5%96-3776ab?style=flat-square)](https://www.python.org/)
[![Platform](https://img.shields.io/badge/Platform-Android%20(arm64)-3ddc84?style=flat-square)](#)
[![License](https://img.shields.io/badge/License-GPLv3-blue?style=flat-square)](LICENSE)

**一句话**：把安卓手机里那些「丢了就再也找不回来」的分区，一键备份到电脑。

**全设备通用** —— 不绑定任何机型，任何已 root 的安卓手机都能用。平台（高通 / 联发科 / 三星 / 展锐 / Tensor）与分区价值由程序**按语义自动推断**，不需要为每台机器写配置。

---

## ⚡ 关于本项目

> **这是一个 100% Vibe Coding 项目。**

项目创建的动机：为了备份安卓手机的重要分区。

项目由DeepSeek完成。

***Ai有可能犯错，仅供vibe coding学习用途，请勿用于工业生产以及其他重要行业中。若出现任何损失，后果自负！！！***

> ⚠️ **使用前请务必阅读 [`DISCLAIMER.md`](DISCLAIMER.md)** —— 写分区是变砖的唯一途径，恢复操作风险极高。

| 环节 | AI 做了什么 | 人工做了什么 |
|---|---|---|
| **调研** | 检索 GitHub 同类项目，确认「带 GUI 的多选备份工具」是市场空白 | 提出需求方向、界定功能范围 |
| **可行性验证** | 写 Spike 脚本实测 `adb exec-out` vs `adb shell` 的二进制完整性 —— 发现 PTY 会把 32MB 数据的 76739 字节污染成 `\r\n` | 提供真机（Redmi K70）供实测 |
| **性能调优** | 对比 4 种取数方式 × 2 种落盘路径，选出 14.8 MB/s 的最优组合 | — |
| **编码** | 三个模块约 3000 行，零第三方依赖 | 逐轮审阅、拍板取舍 |
| **代码审查** | 自查抓出 4 个真实缺陷（含 Tkinter 工作线程读变量这个经典陷阱）| — |
| **自动化测试** | 4 个测试套件共 87 项断言，含专门覆盖 TTY 分支的进度条测试（**开发期回归用，未随仓库发布**）| — |
| **⭐ 人工测试** | 依据人工反馈定位根因、修复并回归验证 | **人类亲手跑测试**：真机上完整走 GUI 备份流程、换一台电脑（虚拟机）验证便携版可移植性 |
| **实机验证** | 与人工备份逐字节 sha256 比对，18/18 完全一致 | 真机端到端跑通并验收结果 |

**但也请理性看待**：AI 也会犯错。开发**早期**实机测试就抓出过 AI 写的缺陷：

  1. A/B 槽位后缀没剥离导致 40+ 分区"未分类"
  2. by-name 里的整盘符号链接被当成分区
  3. 规则表覆盖不全
  4. 出现备份失败的风险

而到了**人工真机验收**阶段，又抓出 3 个更隐蔽、自动化测试完全覆盖不到的缺陷 —— 详见下节 👇

### 🧑🔬 测试由 AI 与人工**共同**完成

自动化测试由 AI 编写（4 个套件 / 87 项断言，**仅开发期使用，未随仓库发布**），但**人工测试不是走过场，而是抓 Bug 的主力**。

下面这 3 个缺陷，AI 的 87 项断言**一个都没覆盖到**，全部是**人类亲手测试**后暴露的：

| # | 人工做的测试动作 | 暴露出的缺陷 |
|---|---|---|
| 1 | 在真机上跑**完整 GUI 备份流程** | 6 个磁盘的 GPT 尾部备份**全部失败**，界面却只显示「通过 20/共 26 项」。根因是把 Windows 本地路径当成了设备路径 —— 这条代码路径 AI 的回归测试从没真正跑过 |
| 2 | **换一台电脑**（虚拟机）跑完整便携版 | `/data/adb` 环境包失败（39/40）。便携包在**陌生环境**下的真实表现，只有换机才验得出来 |
| 3 | 审视失败时的界面反馈 | 环境包失败时**看不出原因**，只有一个红叉 → 促成流式回传与完整性校验的加入 |

> 📌 **结论**：自动化测试能防回归，但**发现不了「AI 想当然」那类错误**。
> 这个工具之所以敢给别人用，靠的是**人真的把它拿去用了**。

**所有代码都经过真机（Redmi K70）验证，其他手机请自行评估风险后再用于备份重要数据（不确保在其他手机上能够正常使用）。**

---

## 一、快速开始

### 环境要求

- **Python 3.8+**（含 tkinter，官方安装包默认自带）
- **ADB** —— 从 [platform-tools](https://developer.android.com/tools/releases/platform-tools) 下载，
  放到 `adb/adb.exe`（Windows）或 `adb/adb`（Linux/macOS），或直接加进系统 PATH
- 一台**已 root** 的安卓手机（Magisk / KernelSU / APatch 均可）

> 本仓库不含 ADB 二进制（8 MB 的二进制进 git 历史后无法瘦身），请自行下载。

### 运行

```bash
git clone <本仓库地址>
cd 安卓分区备份工具          # 或你 clone 下来的目录名

# 把 platform-tools 里的 adb 放进来（三选一）
#   A. 复制到 adb/adb.exe
#   B. 加进系统 PATH
#   C. 什么都不做 —— 程序会自动从 PATH 找

python backup_gui.py         # 启动图形界面
```

Linux / macOS 用户可直接用附带的启动脚本（会自动挑一个带 tkinter 的 Python）：

```bash
./run.sh
```

命令行版（无界面，适合脚本化）：

```bash
python backup_core.py --adb <adb路径> --preset critical --out <输出目录>
python backup_core.py --adb <adb路径> --list        # 只列出设备分区与分级
```

### 手机侧准备

1. 设置 → 关于手机 → 连点「版本号」7 次开启开发者选项
2. 开发者选项 → 打开 **USB 调试**
3. 用数据线连电脑，手机上弹出「允许 USB 调试」→ **勾选「一律允许」并确定**
4. **解锁手机屏幕**（锁屏状态下 adb 常被系统拦截）
5. 确保手机已 root，并在 root 管理器里给 **Shell** 授权

程序会自动识别设备状态，并在界面上提示当前卡在哪一步。

---

## 二、备份什么 —— 四级分类

程序不看机型，而是**按分区名的语义**自动分级：

| 级别 | 含义 | 典型分区 | 丢了会怎样 |
|---|---|---|---|
| **★ Tier 1 不可再生** | 含设备唯一数据 | `persist` `modemst1/2` `fsg` `nvdata` `efs` `devinfo` `frp` `secdata` | **永久损坏，官方固件也救不回** |
| **Tier 2 root 基准** | 你修补过的系统分区 | `init_boot_a/b` `boot_a/b` `vbmeta*` `dtbo*` | 官方只有未修补版，root 状态丢失 |
| **Tier 3 固件可重建** | 官方刷机包里就有 | `system` `vendor` `abl` `modem` `super` | 重刷官方包即可 |
| **Tier 4 低价值** | 厂商备份槽/日志/对齐 | `bk*` `ALIGN*` `logfs` `oops` `rawdump` | 无意义 |

**预设方案**（界面上一键切换）：

| 预设 | 内容 | 典型体积 |
|---|---|---|
| **关键分区（推荐）** | 只含 Tier 1 | ~80 MB |
| **关键 + root 基准** | Tier 1 + Tier 2（默认）| ~1 GB |
| 全部有价值分区 | Tier 1+2+3 | 数 GB ~ 数十 GB |
| 全部分区 | 所有 | 可达 200 GB+ |

> 📚 **分级规则经过外部核实**：本文的分区名单与 [onfix.cn《手机字库基带使用命令备份指南》](https://onfix.cn/course/4780?bid=1&mid=28642)
> 独立列出的名单逐条一致（高通 `fsg/fsc/modemst1/modemst2`；联发科 `nvram/nvdata/nvcfg/persist/protect1/protect2/seccfg`）。
> 该来源同时印证了「联发科的 by-name 路径与高通不同，需要多路径探测」这一点。

> ⚠️ `userdata`（个人数据，可达数百 GB）、`super`（动态分区容器）、`metadata`（加密元数据）
> **永远不会被预设自动勾选**，需要你手动选中 —— 避免"全选"顺手带上几百 GB。

---

## 三、备份文件放哪儿

```
安卓分区备份工具_便携版\
└── Backups\                        ← 程序目录下自动创建
    ├── 我的备份\                    ← 你自定义的名称
    │   ├── .device_info            ← 设备指纹（用于判断"同一台设备"）
    │   ├── README.md               ← 自动生成：设备信息 + 差异对比 + 文件清单
    │   ├── manifest.txt            ← 逐项 SHA256
    │   ├── backup_log.txt          ← 本次完整日志
    │   ├── img\                    ← 分区镜像
    │   └── gpt\                    ← GPT 分区表（头/尾/sgdisk格式/布局文本）
    └── 我的备份_20260918\           ← 同名再次备份 → 自动加日期
```

### 命名与冲突规则

名称由你在界面上**自定义**（留空则用默认名 `Backup`）。同名时按以下顺序消解，**绝不覆盖任何已有备份**：

| 情况 | 结果 |
|---|---|
| 该名称尚未使用 | `我的备份` |
| 已存在，且是**同一台设备** | `我的备份_20260918` ← 追加日期 |
| 已存在，但是**别的设备** | `我的备份_20260918` ← 同样加日期，不把两台机器混一起 |
| 同一天又备了一次 | `我的备份_20260918_153000` ← 再加时分秒 |
| 还是冲突 | `我的备份_20260918_153000_2` ← 再加序号 |

**界面会实时预览最终路径**，并在备份前告诉你为什么是这个名字。

> 🛡️ 自定义名称会经过安全净化（`..\..\Windows` → `____Windows`），
> 确保备份永远落在你指定的根目录之内。

---

## 四、🛡️ 安全声明

**本工具只做只读导出。**

| 项目 | 说明 |
|---|---|
| 读 / 写 | 对设备分区**只有** `dd if=<分区>` 读取，**全代码不存在任何写设备分区的路径** |
| 设备端写入 | 唯一的写入是 `/sdcard/.apb_tmp` 与 `/data/local/tmp` 下的临时文件，**用完立即删除** |
| 覆盖已有备份 | **不可能** —— 创建目录用 `exist_ok=False`，重名自动改名 |
| 命令注入 | 分区名经过白名单正则 `^[A-Za-z0-9_.\-]{1,64}$` 校验 |
| 路径穿越 | 自定义名称过滤 `/ \ ..` 与 Windows 非法字符 |
| 卡死保护 | 传输停滞 120 秒自动中止（防止 adb 挂住导致无限等待）|
| 取消 | 随时可取消，已完成的分区文件保留 |

**恢复功能不提供** —— 写分区是变砖的唯一途径。参考命令只写进每次备份生成的**备份目录**里的 `README.md`，需要时自行手工执行。

本项目绝对不泄露任何个人信息。
---

## 五、校验机制

每备份一个分区，程序都会：

1. **传输**：`adb exec-out` 把 `dd` 的输出**直接重定向**到本地文件
2. **字节数核对**：收到的字节数必须与 `blockdev --getsize64` 报的分区大小**完全一致**
3. **本地 SHA256**：对落盘文件流式计算哈希
4. **GPT 结构验证**（GPT 项）：纯 Python 解析 MBR `55AA` + GPT `EFI PART` 签名，
   并做 head ↔ tail 交叉验证（两者必须互相指向）
5. **可选设备端二次校验**：勾选后额外让设备自己算一遍 `sha256sum`，与本地比对

> 一旦字节数不符，会自动**回退**到「设备端暂存 + adb pull」这条更慢但更稳的路径重试。

### 为什么必须用 `exec-out` 而不是 `shell`？

`adb shell` 会分配 PTY，把二进制流里的 `\n` 转换成 `\r\n`，**静默损坏数据**。

实测数据（32 MB 的 persist 分区）：

| 方式 | 收到字节数 | SHA256 |
|---|---|---|
| `exec-out` | 33554432 | ✅ 与设备完全一致 |
| `shell (PTY)` | **33631171**（多 76739 字节）| ❌ 完全不匹配 |

---

## 六、界面与进度显示

### 图形界面

窗口从上到下是四个区块，**设备信息和 ADB 开关都在最顶部**：

```
① 设备状态 ──────────────────────────────────────────────────────────
  ●  已连接  6f06de3d                          [刷新设备] [检查 Root]
     代号 vermeer · 识别为 高通 Qualcomm（得分 16） · root ✓ KernelSU

  系统 Android 16（SDK 36）  系统版本 OS3.0.307.0.WNKCNXM
  芯片 骁龙 8 Gen 2（SM8550）· 高通          平台 kalama
  内核 5.15.194-android13-8-00019-gf4321180a397-ab15212794
  架构 arm64-v8a             内存 14.8 GB      屏幕 1440x3200
  槽位 _a（当前系统槽）       补丁 2026-08-01   Root ✓ uid=0（u:r:ksu:s0）

  ADB 服务: ● 运行中  [停止]   退出本程序时会自动停止 ADB 服务
──────────────────────────────────────────────────────────────────────

④ 进度与日志 ────────────────────────────────────────────────
  persist      [████████████░░░░░░░░░░░░░░]   48.2%
               15.4 MB / 32.0 MB   11.9 MB/s   剩 2s
  总计 5/13 项 [██████░░░░░░░░░░░░░░░░░░░░]   36.0%
               36.0 MB / 100.0 MB   7.2 MB/s   剩 8s
  ┌──────────────────────────────────────────────────────┐
  │ [21:19:10] ===== 备份 13 个分区 =====                 │  ← 紫色标题
  │ [21:19:10] 开始 persist  (32.0 MB)  [★不可再生]       │
  │ [21:19:13]   [OK] persist  32.0 MB  2.7s  11.9MB/s   │  ← 绿色成功
  │ [21:19:14]   [!] 流式失败，回退到设备端暂存模式 ...    │  ← 黄色告警
  │ [21:19:20]   [X] modemst1 失败: 传输停滞超过 120 秒   │  ← 红色错误
  └──────────────────────────────────────────────────────┘
```

#### 设备信息栏

读的全是**系统自己的属性**（`getprop` + `uname` + `/proc/meminfo` + `wm size`），一次往返读完，不改动设备上任何东西：

| 字段 | 来源 | 说明 |
|---|---|---|
| 系统 | `ro.build.version.release` / `.sdk` | 如 `Android 16（SDK 36）` |
| 系统版本 | `ro.build.version.incremental` | 如 `OS3.0.307.0.WNKCNXM` |
| 芯片 | `ro.soc.model` / `ro.soc.manufacturer` | Android 12+ 的系统权威值 |
| 平台 | `ro.board.platform` | 高通内部代号，如 `kalama` |
| 内核 | `uname -r` | |
| 架构 | `ro.product.cpu.abi` | 如 `arm64-v8a` |
| 内存 | `/proc/meminfo` MemTotal | |
| 屏幕 | `wm size` | 物理分辨率 |
| 槽位 | `ro.boot.slot_suffix` | A/B 设备当前系统槽 |
| 补丁 | `ro.build.version.security_patch` | |
| Root | `su -c id` | 含 SELinux 上下文 |

> 🔍 **芯片型号不猜** —— 优先用 `ro.soc.model`；只有在它缺失时才查一张**很小的**
> 平台代号对照表（`kalama → 骁龙 8 Gen 2`），且一定把原始代号一并显示出来供你核对。
> 查不到就照实显示原始值，取不到的字段一律显示 `—`，不编。
>
> ⚠️ **「系统版本」用的是 `ro.build.version.incremental`，不是 `ro.build.display.id`。**
> 后者在小米/红米上是 **AOSP 构建号**（真机实测为 `BP2A.250605.031.A3`），
> 不是你在「设置 → 关于手机」里看到的那个版本号 —— 一开始就取错了这个，真机验证才发现。
> 同样地，内核以 `uname -r` 为准：有些 ROM 的 `ro.kernel.version` 只有 `5.15`
> 这种两位短值，让它抢先的话界面上就只剩个 `5.15` 了。
>
> 📐 这一排用 `FlowFrame` 铺，**窗口窄了自动折行**，不会丢字段
> （真机 1060px 宽时 3 行；760px 时 4 行，实测无越界）。
> 内核/内存/屏幕走的是 `shell` 而不是 `su` —— 这几项普通权限就能读，
> **没 root 的设备也能看到**。

**四条进度条 / ETA 设计要点**：

| 特性 | 说明 |
|---|---|
| **两条进度条** | 上=当前分区，下=总体（含所有已选分区 + GPT + 环境包）|
| **双 ETA** | 当前分区剩余时间 + 整体剩余时间 |
| **速度平滑** | 用指数滑动平均，不是瞬时值 —— 否则数字会乱跳 |
| **窗口标题同步** | 标题栏显示 `备份中 36%`，最小化后看任务栏也能知道进度 |
| **分阶段切换** | 传输时是百分比进度条；SHA256 校验、GPT 读取、tar 打包时自动切成来回滚动条 |
| **日志分级着色** | 紫=阶段标题，绿=成功，红=失败，黄=告警，灰=细节 |

> ⚠️ 着色判定**前缀标记优先于关键词**。像 `[!] 流式失败，回退到设备端暂存模式`
> 这种带"失败"字样的告警（回退后其实成功了），若先做关键词匹配就会被误染成红色。

### 命令行版

```sh
python backup_core.py --adb <adb路径> --preset critical --out <输出目录>
```

终端里是单行刷新进度条（`\r` 覆盖，不刷屏）：

```
  persist              [████████████░░░░░░░░░░░░░░]  48.2%   15.4 MB/32.0 MB   11.9 MB/s  剩 2s
      ── 总进度  36.0%   36.0 MB / 100.0 MB   平均 7.2 MB/s   剩 8s
```

**输出被重定向到文件时自动降级** —— 只在每跨越 10% 时记一行，避免把成千上万个
`\r` 塞进日志文件。加 `--quiet` 可完全关闭进度输出。

---

### 窗口自适应（响应式布局）

界面在**任何窗口尺寸、任何分辨率、任何 DPI 缩放下都不会有元素被裁掉**。

| 机制 | 解决什么 |
|---|---|
| **窗口尺寸按内容与屏幕实测决定** | 不再写死 `1060x820`。启动时量出内容的真实需求，再夹到屏幕可用区内 |
| **可滚动画布兜底** | 窗口比内容小时自动出现滚动条，保证每个控件都够得着 |
| **横排控件自动折行** | 预设方案、批量按钮、选项开关等在窄窗口下折到下一行，而不是被挤出可视区 |
| **分区表格 / 日志双滚动条** | 窗口很窄时横向可拖，列与长日志行不会被切掉 |

> **为什么不能用 `pack(side="left")`** —— 容器宽度不足时，`pack` 会把**排在后面的控件直接挤出可视区**，既不报错也不提示。表现就是「把窗口调窄之后有些按钮不见了」。
> **为什么也不能用 `grid`** —— `grid` 的**列宽是整个容器共享的**：折行后某个宽控件占了第 0 列，这一列就被撑宽，**其它行也跟着右移**。把列号按行号错开也没用，因为第 1 行仍然要排在第 0 行那些列之后。
> 最终方案：`FlowFrame`（自己算坐标折行）+ `ScrollHost`（滚动兜底）。

### ADB 服务开关

界面上有一行 **`ADB 服务: ● 运行中  [停止]  退出本程序时会自动停止 ADB 服务`** ——
**就在最顶部的「① 设备状态」里**，和「刷新设备 / 检查 Root」挨着，随手就能启停。

| 控件 | 作用 |
|---|---|
| 状态文字 | 实时显示 adb 服务端在不在跑（每 2.5 秒查一次，毫秒级端口探测） |
| `[启动]` / `[停止]` | 手动启停 adb 服务端 |
| 退出时自动停止 | **无条件执行**，不需要设置 —— 关窗时自动 `adb kill-server` |

**为什么要给这个开关** —— adb 服务端（监听 `5037`）是**多个工具共用**的常驻进程（Android Studio、scrcpy 用的是同一个）。所以本程序运行期间不会擅自杀它（只在你显式点「停止」时停），但**退出时一定会停掉**，不留残留进程。

> ⚠️ **点「停止」会同时暂停设备检测** —— 否则轮询线程下一轮的 `adb devices` 会立刻把服务端又拉起来，表现就是「点了停止没反应」。恢复点「启动」即可。

#### 关窗时会做四件事

```
① poll_stop.set()         让后台线程别再发起新的 adb 调用
② kill_live_children()    杀掉还在跑的 adb 子进程
③ adb kill-server         停掉服务端（无条件，且必须在 ② 之后）
④ 等后台线程退出（≤0.6s）  然后 destroy()
```

> ❗ **为什么必须自己清** —— adb 子进程是**独立进程**，Python 退出不会顺带把它们带走。关窗时若正有一次 adb 调用在飞（设备轮询、读分区表、或一次正在进行的备份），那个 `adb.exe` 就会变成孤儿留在任务管理器里。
> 实测（Windows / Python 3.14）：`Popen(["adb","wait-for-device"])` 之后直接让解释器退出，该 adb 进程**依然存活**。
> 实现上 `backup_core.py` 有一个子进程登记表，7 处 `subprocess.run` 全部改走 `_run()`（它会登记 PID），2 处 `Popen` 补了登记，另加 `atexit` 兜底 —— 无论从哪条路径退出都不会留孤儿。

## 七、常见问题

### 「未检测到设备」

界面上会显示具体卡在哪一步：

| 界面提示 | 含义 | 怎么办 |
|---|---|---|
| 未检测到设备 | adb 看不到任何设备 | 换数据线（要能传数据的）/ 装 USB 驱动 |
| **未授权** | 手机上没点"允许" | 看手机屏幕，点「允许 USB 调试」；不行就重新插拔 |
| **离线 (offline)** | 连接不稳定 | 重新插拔，或重启手机 |
| root 不可用 | `su` 被拒绝 | 在 Magisk/KernelSU/APatch 里给 Shell 授权 |

### 中文显示成方块

极少见。若出现，说明系统缺少中文字体，装一个「微软雅黑」即可。

### 备份很慢

实测约 **12-15 MB/s**（受 USB 与 adb 限制）。参考耗时：

| 内容 | 体积 | 耗时 |
|---|---|---|
| 关键分区（Tier 1）| ~80 MB | 约 10 秒 |
| 关键 + root 基准 | ~1 GB | 约 1.5 分钟 |
| `super` | 9 GB | 约 12 分钟 |
| `userdata` | 226 GB | 约 4.5 小时 |

### 想换备份位置

界面上「备份根目录」可以改。默认是程序目录下的 `Backups\`；
若程序装在 Program Files 等只读位置，会自动退回家目录的「文档」文件夹。

---

## 八、技术架构（给未来的维护者）

```
backup_gui.py          图形界面（Tkinter），只负责显示与交互
  ├─ 设备轮询线程       每 1.5 秒查一次 adb devices
  ├─ 消息队列 queue     工作线程 → 主线程的唯一通道
  └─ root.after 消息泵  主线程每 80ms 取一次队列并刷新界面

backup_core.py         核心引擎（无 GUI 依赖，可 CLI 独立运行）
  ├─ Adb                adb.exe 封装 + 能力探测
  ├─ BackupEngine       备份流程编排
  ├─ GPT 解析器         纯 Python 解析 GPT，不依赖设备端的 sgdisk
  └─ 目录命名/冲突消解   sanitize_folder_name / resolve_backup_dir

partition_profiles.py  平台特征 + 分区四级分类规则（纯数据）
```

### 三条不可违背的设计约束

1. **绝不从工作线程碰 tkinter**
   所有 `BooleanVar.get()` 之类的调用必须在主线程完成后再传给工作线程 ——
   否则会随机死锁或崩溃。这是 Tkinter 最经典的坑。

2. **必须剥离 A/B 槽位后缀再匹配规则**
   规则表按无槽位名写（`abl`），设备上实际叫 `abl_a`。
   不剥后缀会导致大量分区"未分类"。见 `classify()`。

3. **GPT 偏移必须在 Python 里算**
   shell 的 `[ ]` 比较对 253 GB 这种数值会 **32 位溢出**成负数，
   导致尾部 GPT 被静默跳过。Python 原生 64 位，没有这个问题。

### 如何支持新平台

在 `partition_profiles.py` 的 `PLATFORMS` 里加一条平台特征，
再往 `TIER1_RULES` 等规则表里补 `(fnmatch 模式, 说明)` 即可，**无需改动任何其他代码**。

---

## 九、版本

| 项目 | 值 |
|---|---|
| 工具版本 | **1.1.1** |
| 核心版本 | 1.0.0 |
| 画像库版本 | 1.0.0 |
| 依赖 | 仅 Python 标准库（tkinter），**零第三方包** |
| 便携包体积 | 约 50 MB（含 Python 运行时 + ADB）|

### 更新记录

| 版本 | 变化 |
|---|---|
| **1.1.1** | **顶部新增详细设备信息栏**（系统/芯片/构建/平台/内核/架构/内存/屏幕/槽位/补丁/Root，窄窗口自动折行）；**ADB 服务开关移到顶部**；**ADB 服务改为退出时无条件停止**（去掉勾选框）；**修复关窗后 `adb.exe` 变成孤儿进程**；**修复拖拽窗口卡顿**（每帧 146ms → 54ms）；**修复窗口缩放后界面元素被隐藏** —— 改为响应式布局：横排控件自动折行、可滚动画布兜底、窗口尺寸按屏幕实测决定 |
| 1.1.0 | 修复 6 个 LUN 的 GPT 尾部全部失败（本地路径被当成设备路径）；修复字节流被 stderr 污染；修复两处白名单校验缺口 |

---

## 十、开源协议

本项目采用 **GNU General Public License v3.0（GPL-3.0）**。

| 项目 | 说明 |
|---|---|
| 许可证 | **GPL-3.0** —— 全文见 [`LICENSE`](LICENSE) |
| SPDX 标识 | `GPL-3.0` |
| 附加声明 | [`DISCLAIMER.md`](DISCLAIMER.md) —— 风险提示，**不修改、不替代** GPL 条款 |

**你可以自由地**：使用、修改、分发本软件，**包括商业用途**。

**但必须**：

- 📌 保留版权声明与许可证原文
- 🔓 **衍生作品也必须以 GPL-3.0 开源**（copyleft —— 这是 GPL 与 MIT 最大的区别）
- 📝 修改过的版本需注明改动内容与日期

**本软件不提供任何担保** —— 详见 [`LICENSE`](LICENSE) 第 15–17 节。

---

## 十一、调研与参考

动手写代码之前，先在 GitHub 上检索了同类项目，也查了分区名单的公开来源。这一节把两件事都交代清楚。

### 同类项目（调研时发现）

调研找到 **8 个相关项目**，全部浏览过。结论是：现存方案**要么是设备端 CLI、要么是 PC 端命令行**，没有一个提供「PC 端 GUI + 分区多选 + 一键备份 + 自动校验」。

列在这里，一是交代调研过程，二是**方便你按需选型** —— 如果你的场景更适合其中某一个，直接用它就好。

| 项目 | 语言 | 形态 | 特点 |
|---|---|---|---|
| [Magisk-Modules-Alt-Repo/backup](https://github.com/Magisk-Modules-Alt-Repo/backup) | Shell | 设备端 CLI | 无需第三方 recovery 的分区备份，同类里最主流 |
| [RuslanUC/pyAdbBackup](https://github.com/RuslanUC/pyAdbBackup) | Python | PC 端 CLI | 定位与本工具最接近，但无图形界面 |
| [zzzee6/Geek_Toolbox](https://github.com/zzzee6/Geek_Toolbox) | Shell | 设备端 | KernelSU / Magisk 下的分区备份与恢复 |
| [Skecthware/Android-Firmware-Dumper](https://github.com/Skecthware/Android-Firmware-Dumper) | — | 设备端 | A/B 槽检测 + MD5 校验 + TAR 打包 |
| [SysAdminDoc/Devicer](https://github.com/SysAdminDoc/Devicer) | C# | Windows WPF | **有 GUI**，但面向三星且功能混杂 |
| [lopestom/android-partition-backup](https://github.com/lopestom/android-partition-backup) | Python | CLI | 纯 ADB + root |
| [Bruh938/Samsung-Partition-Backup-Tool](https://github.com/Bruh938/Samsung-Partition-Backup-Tool) | Python | CLI | 三星专用 |
| [VioletChann/cy-android-toolkit](https://github.com/VioletChann/cy-android-toolkit) | — | 一体工具箱 | ADB / Fastboot / Magisk 全家桶 |

### 分区分级规则的来源

分级规则不是凭空写的 —— **先查公开资料，再用真机验证**。以下来源直接影响了 `partition_profiles.py` 里的规则表：

| 来源 | 贡献了什么 |
|---|---|
| [onfix.cn《手机字库基带使用命令备份指南》](https://onfix.cn/course/4780?bid=1&mid=28642) | 高通 `fsg` `fsc` `modemst1` `modemst2`；联发科 `nvram` `nvdata` `nvcfg` `persist` `protect1` `protect2` `seccfg`。Tier 1 规则与该名单**逐条一致** |
| android-hilfe.de —— 三星 SM-N986B (Note 20 Ultra) IMEI 修复帖 | 三星 `efs` / `sec_efs` 双分区结构 |
| [CamsShaft/arbitrary-R-W-for-steady-and-param-partitions](https://github.com/CamsShaft/arbitrary-R-W-for-steady-and-param-partitions) | 三星 `steady` / `param` 分区的存在性 |

以下几项**未能找到公开来源核实**，属于领域知识保留，已在 `partition_profiles.py` 注释中标注：

> 展锐 `prodnv` `nvitem` `wcnmodem` `splloader`；三星 `up_param`；Tensor `ldfw`

这些规则**模式不匹配时不会误伤**，但若在你的机型上判断有误，欢迎提 Issue。

### 声明

> ⚠️ **本工具没有使用、复制或改写上述任何项目的代码。**
> 三个 `.py` 文件共约 3000 行**全部手写**，**零第三方依赖** —— 你甚至不需要 `pip install` 任何东西。
> 上述项目与来源**仅用于调研和规则核对**，具体实现、分级阈值与推断逻辑均为独立完成。
